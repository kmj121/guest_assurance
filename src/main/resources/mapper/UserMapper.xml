<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.vsc.guest_assurance.dao.UserMapper">
    <resultMap id="BaseResultMap" type="com.vsc.guest_assurance.entity.User">
        <id column="id" property="id" jdbcType="INTEGER"/>
        <result column="user_name" property="userName" jdbcType="VARCHAR"/>
        <result column="email" property="email" jdbcType="VARCHAR"/>
        <result column="is_manager" property="isManager" jdbcType="INTEGER"/>
    </resultMap>
    <sql id="Base_Column_List">
        id, user_name, email, is_manager
    </sql>
    <select id="selectByPrimaryKey" resultMap="BaseResultMap" parameterType="java.lang.Integer">
        select
        <include refid="Base_Column_List"/>
        from user
        where id = #{id,jdbcType=INTEGER}
    </select>
    <delete id="deleteByPrimaryKey" parameterType="java.lang.Integer">
        delete
        from user
        where id = #{id,jdbcType=INTEGER}
    </delete>
    <insert id="insert" parameterType="com.vsc.guest_assurance.entity.User">
        insert into user (id, user_name, email,
                          is_manager)
        values (#{id,jdbcType=INTEGER}, #{userName,jdbcType=VARCHAR}, #{email,jdbcType=VARCHAR},
                #{isManager,jdbcType=INTEGER})
    </insert>
    <insert id="insertSelective" parameterType="com.vsc.guest_assurance.entity.User">
        insert into user
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="id != null">
                id,
            </if>
            <if test="userName != null">
                user_name,
            </if>
            <if test="email != null">
                email,
            </if>
            <if test="isManager != null">
                is_manager,
            </if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="id != null">
                #{id,jdbcType=INTEGER},
            </if>
            <if test="userName != null">
                #{userName,jdbcType=VARCHAR},
            </if>
            <if test="email != null">
                #{email,jdbcType=VARCHAR},
            </if>
            <if test="isManager != null">
                #{isManager,jdbcType=INTEGER},
            </if>
        </trim>
    </insert>
    <update id="updateByPrimaryKeySelective" parameterType="com.vsc.guest_assurance.entity.User">
        update user
        <set>
            <if test="userName != null">
                user_name = #{userName,jdbcType=VARCHAR},
            </if>
            <if test="email != null">
                email = #{email,jdbcType=VARCHAR},
            </if>
            <if test="isManager != null">
                is_manager = #{isManager,jdbcType=INTEGER},
            </if>
        </set>
        where id = #{id,jdbcType=INTEGER}
    </update>
    <update id="updateByPrimaryKey" parameterType="com.vsc.guest_assurance.entity.User">
        update user
        set user_name  = #{userName,jdbcType=VARCHAR},
            email      = #{email,jdbcType=VARCHAR},
            is_manager = #{isManager,jdbcType=INTEGER}
        where id = #{id,jdbcType=INTEGER}
    </update>

    <select id="selectList" resultType="com.vsc.guest_assurance.vo.BackendUserListVo">
        select id as userId, email, user_name as userName, is_manager as privilege
        from user
        where 1 = 1
        <if test="email != null and email != ''">
            and email like '%'+#{email}+'%'
        </if>
        <if test="userName != null and userName != ''">
            and user_name like '%'+#{userName}+'%'
        </if>
        <if test="privilege != null">
            and is_manager = #{privilege}
        </if>
    </select>
</mapper>